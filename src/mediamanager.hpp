#ifndef MEDIAMANAGER_H
#define MEDIAMANAGER_H

#include <iostream>
#include <string>

class MediaManager
{
  public:
    MediaManager();
    ~MediaManager();
    void loadMedia(std::string filename);
  private:
};

#endif //MEDIAMANAGER_H
